import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-issue336',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
